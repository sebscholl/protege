# Responsibilities

Store scheduler responsibility definitions here.

Each responsibility is one markdown file named `<responsibility_id>.md`.

Required frontmatter fields:

1. `name`
2. `schedule` (cron)
3. `enabled`

The markdown body is the prompt executed on each scheduled run.

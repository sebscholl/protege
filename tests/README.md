# Tests

Extension Surface: Yes

Test suite for unit, integration, and regression coverage.

Network interception utilities are defined in `tests/network/`.

Top-level test areas:

1. `tests/engine/` for module-level runtime behavior.
2. `tests/relay/` for optional relay service behavior.
3. `tests/e2e/` for cross-system integration smoke coverage.

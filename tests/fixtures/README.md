# Shared Fixtures

Extension Surface: Yes

All test fixtures live under this directory.

Layout:

1. `tests/fixtures/email/` for MIME email fixtures (`.eml`).
2. `tests/fixtures/api/` for network API fixtures (`.json`).
3. `tests/fixtures/apps/` for baseline app workspace templates copied into temp dirs for isolated integration/e2e tests.
